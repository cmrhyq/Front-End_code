import React from 'react';
import { TdNotificationProps } from './type';
import { StyledProps } from '../common';
export interface NotificationProps extends TdNotificationProps, StyledProps {
    id?: string;
}
export declare const Notification: React.ForwardRefExoticComponent<NotificationProps & React.RefAttributes<any>>;
export default Notification;
