import React from 'react';
import { DropdownOption } from '../type';
export declare const getOptionsFromChildren: (children: React.ReactElement) => DropdownOption[];
export default function useDropdownOptions(children: (React.ReactChild | React.ReactFragment | React.ReactPortal)[], options: DropdownOption[]): DropdownOption[];
