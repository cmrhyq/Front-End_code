import { FC } from 'react';
import { TdTimePickerProps, TimeRangePickerPartial } from '../type';
export interface SinglePanelProps extends Pick<TdTimePickerProps, 'steps' | 'format' | 'value' | 'hideDisabledTime' | 'onChange'> {
    disableTime?: (h: number, m: number, s: number, context?: {
        partial: TimeRangePickerPartial;
    }) => Partial<{
        hour: number[];
        minute: number[];
        second: number[];
    }>;
    position?: TimeRangePickerPartial;
    triggerScroll?: boolean;
    resetTriggerScroll?: () => void;
    isVisible?: boolean;
}
declare const SinglePanel: FC<SinglePanelProps>;
export default SinglePanel;
