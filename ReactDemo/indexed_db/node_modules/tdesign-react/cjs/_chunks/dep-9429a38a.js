/**
 * tdesign v0.41.1
 * (c) 2022 tdesign
 * @license MIT
 */

'use strict';

var _typeof = require('./dep-2a90f794.js');
var eq$1 = require('./dep-384b291c.js');
var isArrayLike$1 = require('./dep-d3ad6e52.js');
var _isIndex = require('./dep-9cd0fde8.js');
var isObject$1 = require('./dep-aab723b3.js');

var eq = eq$1.eq_1,
    isArrayLike = isArrayLike$1.isArrayLike_1,
    isIndex = _isIndex._isIndex,
    isObject = isObject$1.isObject_1;
/**
 * Checks if the given arguments are from an iteratee call.
 *
 * @private
 * @param {*} value The potential iteratee value argument.
 * @param {*} index The potential iteratee index or key argument.
 * @param {*} object The potential iteratee object argument.
 * @returns {boolean} Returns `true` if the arguments are from an iteratee call,
 *  else `false`.
 */

function isIterateeCall(value, index, object) {
  if (!isObject(object)) {
    return false;
  }

  var type = _typeof._typeof(index);

  if (type == 'number' ? isArrayLike(object) && isIndex(index, object.length) : type == 'string' && index in object) {
    return eq(object[index], value);
  }

  return false;
}

var _isIterateeCall = isIterateeCall;

exports._isIterateeCall = _isIterateeCall;
//# sourceMappingURL=dep-9429a38a.js.map
